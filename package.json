{
  "name": "kindred-club",
  "version": "1.0.0",
  "private": true,
  "description": "Golf scores, shared rewards and charitable impact",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "stripe": "^22.6.2"
  }
}
